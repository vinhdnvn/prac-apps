name: CI - Build & Push

on:
  push:
    branches: [main]
    paths:
      - "apps/fe/**"
      - "apps/be/**"
  workflow_dispatch:

jobs:
  build-fe:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push FE image
        uses: docker/build-push-action@v5
        with:
          context: apps/fe
          push: true
          tags: |
            ghcr.io/vinhdnvn/fe:latest
            ghcr.io/vinhdnvn/fe:${{ github.sha }}

  build-be:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push BE image
        uses: docker/build-push-action@v5
        with:
          context: apps/be
          push: true
          tags: |
            ghcr.io/vinhdnvn/be:latest
            ghcr.io/vinhdnvn/be:${{ github.sha }}
